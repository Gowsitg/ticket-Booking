import { InputvaluestyleDirective } from './inputvaluestyle.directive';

describe('InputvaluestyleDirective', () => {
  it('should create an instance', () => {
    const directive = new InputvaluestyleDirective();
    expect(directive).toBeTruthy();
  });
});
